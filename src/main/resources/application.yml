spring:
  application:
    name: user-center
    # DataSource Config
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jc_xingqiu
    username: root
    password: admin
server:
  port: 8080
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
